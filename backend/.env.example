# ============================================================
# Django Dishom Project - Environment Variables
# ============================================================
# IMPORTANT: Copy this file to .env and fill in the values for production
# This file should NOT be committed to version control
# ============================================================

# SECURITY Settings
# ============================================================
# CRITICAL: Generate a new secret key for production
# You can generate one with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-secret-key-here-change-this-in-production

# Debug Mode (ALWAYS False in production)
DEBUG=False

# Allowed Hosts (comma-separated list of domains/IPs)
ALLOWED_HOSTS=safalclasses.com,www.safalclasses.com,13.200.248.251

# CORS Settings
# ============================================================
# Allowed origins for CORS (comma-separated)
CORS_ALLOWED_ORIGINS=https://safalclasses.com,https://www.safalclasses.com

# Database Configuration (Optional - uses SQLite by default)
# ============================================================
# If using PostgreSQL in production, uncomment and configure:
# DATABASE_URL=postgresql://user:password@localhost:5432/dishom_db

# Cache Configuration (Optional)
# ============================================================
# Redis URL for caching (if not set, uses local memory cache)
# REDIS_URL=redis://127.0.0.1:6379/1

# Email Configuration (Required for production notifications)
# ============================================================
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-specific-password
DEFAULT_FROM_EMAIL=noreply@safalclasses.com

# Frontend URL
# ============================================================
FRONTEND_BASE_URL=https://safalclasses.com

# ============================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ============================================================
# Before deploying to production, ensure:
# 1. ✓ SECRET_KEY is changed from default
# 2. ✓ DEBUG=False
# 3. ✓ ALLOWED_HOSTS includes your domain
# 4. ✓ Email credentials are configured
# 5. ✓ Database is configured (if not using SQLite)
# 6. ✓ Static files are collected: python manage.py collectstatic
# 7. ✓ Migrations are applied: python manage.py migrate
# 8. ✓ SSL/HTTPS is configured on your server
# 9. ✓ Firewall rules are configured
# 10. ✓ Regular backups are configured
# ============================================================
