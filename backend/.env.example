# ========================================
# Safal Classes - Environment Configuration
# ========================================
# Copy this file to .env and update with your actual values
# DO NOT commit .env file to Git (it's in .gitignore)

# ========================================
# DJANGO CORE SETTINGS
# ========================================

# Secret key for Django (REQUIRED)
# Generate new key: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-super-secret-key-change-this-min-50-chars-random-string

# Debug mode (True for development, False for production)
# IMPORTANT: Set to False in production!
DEBUG=True

# Allowed hosts (comma-separated, no spaces)
# Development: localhost,127.0.0.1
# Production: safalclasses.com,www.safalclasses.com,your-server-ip
ALLOWED_HOSTS=localhost,127.0.0.1,safalclasses.com,www.safalclasses.com


# ========================================
# DATABASE CONFIGURATION (MongoDB)
# ========================================

# MongoDB Database Name
MONGO_DB_NAME=safalclasses_db

# MongoDB Host
# Development: localhost
# Production: localhost or MongoDB Atlas URI
MONGO_HOST=localhost

# MongoDB Port (default: 27017)
MONGO_PORT=27017

# MongoDB Authentication (Optional)
# Leave empty for local development without authentication
# For production, set username and password
MONGO_USER=
MONGO_PASSWORD=

# MongoDB Atlas (Cloud) - Alternative
# If using MongoDB Atlas, you can use full connection string:
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/safalclasses_db?retryWrites=true&w=majority


# ========================================
# EMAIL CONFIGURATION
# ========================================

# Email backend
# Development: django.core.mail.backends.console.EmailBackend
# Production: django.core.mail.backends.smtp.EmailBackend
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Gmail SMTP Settings (for production)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True

# Your Gmail credentials
# For Gmail: Enable 2FA and generate App Password
# https://myaccount.google.com/apppasswords
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-specific-password

# From email address
DEFAULT_FROM_EMAIL=noreply@safalclasses.com


# ========================================
# FRONTEND CONFIGURATION
# ========================================

# Frontend URL
# Development: http://localhost:5173
# Production: https://safalclasses.com
FRONTEND_BASE_URL=http://localhost:5173


# ========================================
# CORS SETTINGS
# ========================================

# Allowed origins for CORS (comma-separated)
# Development: http://localhost:5173,http://127.0.0.1:5173
# Production: https://safalclasses.com,https://www.safalclasses.com
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173


# ========================================
# SECURITY SETTINGS (Production Only)
# ========================================

# These are automatically applied when DEBUG=False
# No need to set them here, they're in settings.py


# ========================================
# OPTIONAL SETTINGS
# ========================================

# Timezone (default: Asia/Kolkata)
# TZ=Asia/Kolkata

# Language (default: en-us)
# LANGUAGE_CODE=en-us


# ========================================
# QUICK SETUP GUIDE
# ========================================

# DEVELOPMENT:
# 1. Copy this file to .env
# 2. Set DEBUG=True
# 3. Keep default EMAIL_BACKEND (console)
# 4. Set FRONTEND_BASE_URL=http://localhost:5173
# 5. Run: python manage.py runserver

# PRODUCTION:
# 1. Copy this file to .env on server
# 2. Set DEBUG=False
# 3. Generate new SECRET_KEY
# 4. Set ALLOWED_HOSTS with your domain
# 5. Configure Gmail SMTP settings
# 6. Set FRONTEND_BASE_URL=https://safalclasses.com
# 7. Set CORS_ALLOWED_ORIGINS=https://safalclasses.com
# 8. Run deployment script
